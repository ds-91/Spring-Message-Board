package com.derek.hackernewsclone.controller;

import com.derek.hackernewsclone.entity.User;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PostMapping;

@Controller
public class RegisterController {

  /*
  @PostMapping("/register")
  public String registerUser(@ModelAttribute User user) {
    // check to make sure user inputs are valid
    // if valid, hash their password
    // save user to database
    // display next page

    // TEMPORARY: display inputs on next page
    //System.out.println("Trying to register user...");
    //return ""
  }
  */
}
